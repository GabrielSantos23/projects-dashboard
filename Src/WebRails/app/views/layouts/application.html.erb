<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Projects Hub" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Project Dashboard — manage and track your local git repositories" %>">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
</head>
<body class="font-sans antialiased" style="background-color: #1a1a1a; color: #a1a1aa;">
  <div class="flex flex-col h-screen text-gray-300 font-sans antialiased" id="app-root">
    <div class="flex flex-1 overflow-hidden">

      <aside id="sidebar" class="w-[220px] shrink-0 flex flex-col border-r border-[#2a2a2a] transition-all duration-300" style="background-color: #161616; overflow-x: hidden;" data-collapsed="false">

        <div onclick="toggleSidebar()" class="px-4 py-5 flex items-center justify-between text-[#d4d4d8] cursor-pointer hover:bg-white/5 transition-colors whitespace-nowrap">
          <div class="flex items-center gap-2">
            <div class="flex gap-0.5">
              <div class="w-2.5 h-2.5 rounded-full bg-white opacity-90"></div>
              <div class="w-2.5 h-2.5 rounded-full bg-white opacity-60"></div>
              <div class="w-2.5 h-2.5 rounded-full bg-white opacity-30"></div>
            </div>
            <span class="sidebar-label text-sm font-medium transition-opacity">Projects Hub</span>
            <svg class="sidebar-label w-3.5 h-3.5 opacity-50 ml-1 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
          </div>
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="sidebar-label shrink-0 w-4 h-4 opacity-40 hover:opacity-100 text-[#d4d4d8]"><rect width="18" height="18" x="3" y="3" rx="2"></rect><path d="M15 3v18"></path><path d="m8 9 3 3-3 3"></path></svg>
        </div>

        <nav class="flex-1 px-3 py-2 space-y-6 overflow-y-auto custom-scrollbar text-[13px] font-medium">
          <div>
            <h2 class="sidebar-label px-2 mb-2 text-[11px] text-[#6b6b70] whitespace-nowrap transition-opacity">General</h2>
            <ul class="space-y-0.5">
              <li>
                <a href="/" class="flex items-center justify-between px-2 py-1.5 rounded-md transition-colors group <%= nav_class('/') %>" title="Dashboard">
                  <div class="flex items-center gap-2.5">
                    <svg class="w-4 h-4 opacity-50 text-white group-hover:opacity-100 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"></path></svg>
                    <span class="sidebar-label whitespace-nowrap transition-opacity">Dashboard</span>
                  </div>
                </a>
              </li>
              <li>
                <a href="/projects" class="flex items-center justify-between px-2 py-1.5 rounded-md transition-colors group <%= nav_class('/projects') %>" title="All Projects">
                  <div class="flex items-center gap-2.5">
                    <svg class="w-4 h-4 opacity-50 group-hover:opacity-100 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"></path></svg>
                    <span class="sidebar-label whitespace-nowrap transition-opacity">All Projects</span>
                  </div>
                  <span class="sidebar-label text-[10px] text-[#6b6b70] shrink-0"><%= total_projects_count %></span>
                </a>
              </li>
              <li>
                <a href="/activity" class="flex items-center justify-between px-2 py-1.5 rounded-md transition-colors group <%= nav_class('/activity') %>" title="Recent Activity">
                  <div class="flex items-center gap-2.5">
                    <svg class="w-4 h-4 opacity-50 group-hover:opacity-100 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                    <span class="sidebar-label whitespace-nowrap transition-opacity">Recent Activity</span>
                  </div>
                </a>
              </li>
            </ul>
          </div>

          <div>
            <div class="sidebar-label flex items-center justify-between px-2 mb-2 whitespace-nowrap transition-opacity">
              <h2 class="text-[11px] text-[#6b6b70]">Favorites</h2>
              <button class="opacity-40 hover:opacity-100 text-white"><svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path></svg></button>
            </div>
            <% if pinned_projects.any? %>
              <ul class="space-y-0.5">
                <% pinned_projects.each do |p| %>
                  <li>
                    <a href="/projects/<%= p.id %>" class="flex items-center justify-between px-2 py-1.5 rounded-md transition-colors truncate <%= nav_class("/projects/#{p.id}") %>" title="<%= p.name %>">
                      <div class="flex items-center gap-2.5 truncate">
                        <div class="w-4 h-4 rounded-full bg-[#2a2a2a] shrink-0 flex items-center justify-center text-[8px] text-white font-bold"><%= p.initial %></div>
                        <span class="sidebar-label truncate transition-opacity"><%= p.name %></span>
                      </div>
                    </a>
                  </li>
                <% end %>
              </ul>
            <% else %>
              <div class="sidebar-label px-2 text-xs text-[#5b5b60] italic whitespace-nowrap transition-opacity">No favorites yet</div>
            <% end %>
          </div>
        </nav>

        <div class="px-3 py-3 border-t border-[#2a2a2a] shrink-0 mt-auto">
          <a href="/settings" class="flex items-center px-1 py-1.5 text-[13px] font-medium rounded-md transition-colors <%= nav_class('/settings') %>" title="Settings">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-4 h-4 mr-2.5 opacity-50 shrink-0"><path d="M11 10.27 7 3.34"></path><path d="m11 13.73-4 6.93"></path><path d="M12 22v-2"></path><path d="M12 2v2"></path><path d="M14 12h8"></path><path d="m17 20.66-1-1.73"></path><path d="m17 3.34-1 1.73"></path><path d="M2 12h2"></path><path d="m20.66 17-1.73-1"></path><path d="m20.66 7-1.73 1"></path><path d="m3.34 17 1.73-1"></path><path d="m3.34 7 1.73 1"></path><circle cx="12" cy="12" r="2"></circle><circle cx="12" cy="12" r="8"></circle></svg>
            <span class="sidebar-label whitespace-nowrap transition-opacity">Settings</span>
          </a>
        </div>
      </aside>

      <main class="flex-1 overflow-y-auto custom-scrollbar" style="background-color: #1a1a1a;">
        <% if flash[:notice] %>
          <div class="mx-6 mt-4 px-4 py-2.5 bg-[#34c759]/10 border border-[#34c759]/20 text-[#34c759] text-[13px] font-medium rounded-lg flex items-center gap-2 animate-fade-in">
            <svg class="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
            <%= flash[:notice] %>
          </div>
        <% end %>
        <% if flash[:alert] %>
          <div class="mx-6 mt-4 px-4 py-2.5 bg-[#ff3b30]/10 border border-[#ff3b30]/20 text-[#ff3b30] text-[13px] font-medium rounded-lg flex items-center gap-2 animate-fade-in">
            <svg class="w-4 h-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
            <%= flash[:alert] %>
          </div>
        <% end %>
        <%= yield %>
      </main>
    </div>
  </div>

  <%= render "shared/scan_modal" %>

  <script>
    function toggleSidebar() {
      const sidebar = document.getElementById('sidebar');
      const labels = sidebar.querySelectorAll('.sidebar-label');
      const collapsed = sidebar.dataset.collapsed === 'true';

      if (collapsed) {
        sidebar.style.width = '220px';
        sidebar.dataset.collapsed = 'false';
        labels.forEach(l => { l.style.display = ''; l.style.opacity = '1'; });
      } else {
        sidebar.style.width = '52px';
        sidebar.dataset.collapsed = 'true';
        labels.forEach(l => { l.style.display = 'none'; l.style.opacity = '0'; });
      }
    }

    document.addEventListener('keydown', function(e) {
      if (e.ctrlKey && e.key.toLowerCase() === 'b') {
        e.preventDefault();
        toggleSidebar();
      }
    });

    function openScanModal() {
      document.getElementById('scan-modal').classList.remove('hidden');
    }
    function closeScanModal() {
      document.getElementById('scan-modal').classList.add('hidden');
    }

    function copyToClipboard(text) {
      navigator.clipboard.writeText(text);
    }
  </script>
</body>
</html>
